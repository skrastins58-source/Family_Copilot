## 🌐 SEO un pieejamības prasības

Family Copilot `landing.html` ievēro šādas prasības:

- Semantiski HTML5 elementi (`<main>`, `<section>`, `<nav>`)
- ARIA lomas (`role="banner"`, `role="navigation"`, `aria-label`)
- Alt teksti visiem attēliem
- Lighthouse pieejamības rādītājs ≥ 90%
- Validācija ar `htmlhint` CI plūsmā
